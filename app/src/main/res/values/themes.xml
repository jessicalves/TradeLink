<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TradeLink" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/font_family</item>
    </style>

    <style name="Theme.TradeLink" parent="Base.Theme.TradeLink" />

    <style name="ButtonWithoutCorner" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_without_corner_background</item>
    </style>

    <style name="TransparentButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>